# Production environment variables for BrewStack POS Frontend
# These values are baked into the static build at compile time by Vite.
# DO NOT store secrets here — this file is safe to commit.

# Base URL of the Spring Boot API in production.
# Update this to your actual server address before deploying.
VITE_API_BASE_URL=http://localhost:8181/api
